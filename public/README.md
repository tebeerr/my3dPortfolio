# Drop your assets here

- `cv.pdf` — your resume (linked from Hero and Footer)
- `favicon.ico` — site icon
- `og-image.png` — 1200×630 Open Graph card

The GitHub avatar is fetched automatically at runtime from the GitHub API
via `/api/github` and rendered with `next/image`, so you don't need a static copy.
